# 阶段 5 开发流程

本文把 `最终方案_独立PythonAgent小说对话标注.md` 中的阶段 5 质量增强拆成可连续开发的四个阶段。阶段 5 的目标不是追求速度，而是在阶段 4 已能整卷无人值守运行的基础上，建立可验证、可回归的质量改进流程。

## 阶段 5-1：评估基线 + 专有词扫描

目标：

- 先让质量变化可量化，再继续改 agent 策略。
- 确认生产逻辑中没有混入当前评测小说的专有角色名、地点、剧情或口癖。

内容：

- 增加评估脚本，对比 `labeled_test.txt` 和 `answers/`，输出准确率、错误行号、模型标签和参考答案。
- 支持生成 `error_labels.txt` 风格的错误明细，便于人工复核。
- 人工答案中的 `角色A|角色B` 表示多个可接受标签，评估时任一匹配都算正确。
- 增加生产逻辑专有词扫描。默认扫描 `dialoop/` 和 `pyproject.toml`，不扫描测试语料、人工答案、样例小说和文档。
- 专有词列表由运行命令或临时文件提供，不写入生产代码。

验收：

- 可以复现前 N 条标注的准确率。
- 可以输出可人工阅读的错误列表。
- 可以证明生产逻辑没有硬编码当前评测小说的专有词。

示例：

```bash
python -m dialoop.quality_cli evaluate \
  --answers answers/ \
  --labels labeled_test.txt \
  --novel novel.txt \
  --error-output error_labels.txt

python -m dialoop.quality_cli scan-terms \
  --term 专有角色名 \
  --term 专有口癖
```

## 阶段 5-2：证据化标注 + annotations.jsonl

目标：

- 让每条标注都能追溯到模型依据，而不是只看到最终 speaker。
- 在不破坏 `labeled.txt` 的情况下，为后续复核和多 agent 协作提供结构化输入。

内容：

- 内部标注结果扩展为 `speaker / evidence_lines / reason / rejected_candidates / confidence`。
- `confidence` 只使用 `high | medium | low`。
- `labeled.txt` 仍只写 speaker，保持兼容。
- 新增 `.dialoop/annotations.jsonl`，保存证据、反证、工具调用摘要和最终 speaker。

验收：

- 单 agent 标注仍能长跑。
- 每条标注都有可读证据。
- 错误样本能看出模型当时为什么会错。

## 阶段 5-3：风险门控 + Verifier Agent

目标：

- 不让所有对话都进入昂贵流程，只把高风险样本交给复核 agent。
- 先解决短句、被称呼者误判、证据不足这些高频错误。

内容：

- 增加 Programmatic Risk Gate，判断便宜、确定、可测试的风险信号。
- 增加 Verifier Agent，专门审查 Labeler 的证据是否支持 speaker。
- Verifier 不负责完整重标，只负责找反证、轮次冲突、被称呼者误判和证据不足。
- 高风险样本经 Verifier 通过后再写入；被否决时进入重读或仲裁。

验收：

- 复核触发原因写入 `annotations.jsonl`。
- 高风险样本准确率相对阶段 5-2 提升。
- 长跑仍能无人值守。

## 阶段 5-4：身份后置查找 + 轻量角色库 / 归一

目标：

- 处理“当前只有临时身份词，后文才揭示姓名或稳定身份”的情况。
- 减少同一人物被标成多个显示名。
- 结合阶段 5-3.5 mismatch attribution 结果，一边提高 Risk Gate 对错例的召回，一边处理 Verifier false pass。

内容：

- 基于 mismatch attribution 报告区分 `medium_or_lower_risk_no_verifier` 和 `high_risk_verifier_pass`，分别作为 Risk Gate 召回与 Verifier 误放行的优化入口。
- 增加 Identity Locator Agent，粗略查找后文可能揭示身份的区域。
- 增加 Identity Resolver Agent，在候选区域内细读并判断是否同一人物。
- 增加轻量角色库，只记录当前文本有证据支持的 `display_name / aliases / summary / evidence_lines / last_seen / confidence`。
- 增加 Name Normalizer，只提出显示名归一建议，不直接改变说话人归属。
- 需要时由 Arbiter Agent 裁决 Labeler、Verifier、Normalizer 之间的冲突。

验收：

- 身份后置样本能给出查找行号和同一人物判断依据。
- 同一重要角色显示名更稳定。
- 普通路人不会被无限向后搜索，也不会被过度合并。
- 5-4 前后的 mismatch attribution 报告能显示中低风险未复核错例与 Verifier false pass 的数量变化。

## 开发原则

- 不把某部小说的角色名、地点、剧情或口癖写进生产逻辑。
- 不用简单投票替代证据比较。
- 不为了速度催促模型过早提交。
- 单个 agent 的任务保持单一，避免一个上下文同时承担定位、细读、复核、归一和仲裁。
- 每个阶段都必须能用阶段 5-1 的评估脚本做回归对照。
