# 小说对话说话人标注工具 — 完整技术方案

> 本文目标读者：任何 AI 模型或开发者，从零开始阅读本文，就能完全理解项目目标和技术方案。

## 1. 项目目标和背景

### 1.1 要做什么

标注小说的对话说话人：
- 输入：.txt 格式小说文件（novel.txt），对话用「」包裹
- 输出：labeled.txt，每行一个名字，按对话出现顺序
- 要求：准确率高，自动循环标注直到全部完成

### 1.2 现有工作流

```
novel.txt → get_dialogue.py → 输出待标注对话 → OpenCode AI → 判断说话人 → write_label.py → labeled.txt
                                    ↑                                      |
                                    +-------- Ralph Loop 自动循环 ---------+
```

用户输入 /ralph-loop 运行 python get_dialogue.py 然后按照程序输出要求完成任务
OpenCode AI 运行 get_dialogue.py 得到输出（如 第23行「这是最后一件了吧？」）
AI 自主调用 read_file 读取 novel.txt 指定行范围
AI 分析上下文判断说话人
AI 运行 python write_label.py --name 罗伦斯
Ralph Loop 检测到空闲，自动发送继续消息
回到第2步，直到完成
已成功标注400+条对话

### 1.3 问题

OpenCode（10万+行）+ openloop 插件不适合分发：
- 需要安装 OpenCode
- 需要安装插件
- 需要打开 TUI 输入提示词
- 对非技术用户门槛高

## 2. OpenCode 深度分析

### 2.1 项目概览

- 仓库：github.com/anomalyco/opencode
- 定位：开源 AI 编程代理（Terminal UI）
- 技术栈：TypeScript / Bun / Effect-TS / Drizzle ORM
- 本地路径：opencode-dev/

### 2.2 关键目录结构

opencode-dev/packages/opencode/src/
  agent/        - Agent 定义
  session/      - 会话管理（核心）
    prompt.ts   - 2157行：LLM调用+工具执行主逻辑
    llm.ts      - 459行：LLM stream 封装
    session.ts  - 1011行：会话CRUD
    processor.ts- 823行：消息处理+工具协调
    status.ts   - 会话状态（session.idle事件）
  tool/         - 工具定义（核心能力来源）
    read.ts     - read_file工具（341行）
    read.txt    - read_file的提示词描述
    shell.ts    - shell执行工具
    registry.ts - 工具注册中心（472行）
    tool.ts     - 工具基类定义（164行）
  plugin/       - 插件系统
  provider/     - LLM提供商适配

opencode-dev/packages/plugin/src/
  index.ts      - 333行：Hooks定义、Plugin类型
  tool.ts       - 插件端工具定义辅助

### 2.3 工具系统 (Tool System)

每个工具是 Tool.Def 对象（来自 tool/tool.ts 第35-49行）：
- id: 工具唯一标识，如 "read", "write", "shell"
- description: 给LLM看的描述
- parameters: 参数schema
- jsonSchema: 转给LLM的JSON Schema
- execute(args, ctx): 实际执行逻辑

关键工具 read_file（来自 tool/read.ts 第29-37行）：
- filePath: 绝对路径
- offset: 起始行号（1-indexed，可选）
- limit: 最大行数（默认2000，可选）

工具返回格式：每行前缀为行号，如 "23: 这是最后一件了吧？"

工具注册中心（来自 tool/registry.ts）：
- 合并内置工具（read, write, edit, shell, grep, glob...）+ 插件工具
- 通过 ToolRegistry.tools() 统一注册给 LLM

### 2.4 Agent 循环（核心机制）

来自 session/prompt.ts 第522-700行：

1. SessionPrompt.resolveTools() 收集所有工具
2. 每个工具转为 AI SDK 的 tool() 对象
3. LLM.stream() 发送 messages + tools 给 LLM
4. 处理 stream 事件：
   - tool-call: LLM想调用工具→执行→结果追加到对话→继续循环
   - finish: LLM完成本轮
5. 如果有工具调用→回到步骤1，否则结束→session进入idle

### 2.5 事件系统

来自 session/status.ts：
- session.idle: 会话空闲（LLM完成回复，没在等工具结果）
- session.deleted: 会话被删除
- command.executed: 命令被执行

openloop 通过监听 session.idle 实现自动循环。

### 2.6 插件 API

来自 plugin/src/index.ts：

Plugin = (input: PluginInput) => Promise<Hooks>

Hooks 包含：
- event?: 监听事件 ← openloop 用这个
- tool?: 注册自定义工具
- config?: 处理配置
- chat.message?: 修改消息
- tool.execute.before/after?: 工具执行前后钩子

PluginInput 包含：
- client: SDK客户端（可调用API）
- directory: 当前项目目录
- worktree: 工作树根目录

## 3. openloop (Ralph Loop) 深度分析

### 3.1 项目概览

- npm包名：@felipejesus/openloop
- 定位：OpenCode自动循环插件
- 技术栈：TypeScript
- 本地路径：openloop-main/
- 核心文件：src/index.ts（仅386行！）

### 3.2 目录结构

openloop-main/
  src/index.ts           - 386行：全部核心代码
  commands/
    ralph-loop.md        - /ralph-loop 命令
    cancel-ralph.md      - /cancel-ralph 命令
    ralph-help.md        - /ralph-help 命令
  skills/
    ralph-loop/SKILL.md  - 告诉AI怎么使用循环
    cancel-ralph/SKILL.md
    ralph-help/SKILL.md
  .opencode/plugins/
    ralph-loop.ts        - 加载入口

### 3.3 核心代码解读

状态管理（第22-188行）：
- 状态文件：.opencode/ralph-loop.local.md
- 格式：YAML front matter + Markdown
- RalphState = { active, iteration, maxIterations(默认100), sessionId, prompt }
- 完成检测：正则 /<promise>DONE<\/promise>/is

事件监听（第308-382行）：
1. command.executed → 处理 /ralph-loop 或 /cancel-ralph
2. session.deleted → 清理绑定的状态
3. session.idle → 核心自动循环：
   a. 检查 active 状态
   b. 匹配 sessionID
   c. 检查是否完成（<promise>DONE</promise>）
   d. 检查迭代上限
   e. 通过 SDK 发送继续消息到同一会话

自动继续的消息发送（第366-381行）：
await client.session.promptAsync({
    path: { id: sessionID },
    body: { parts: [{ type: "text", text: continuationPrompt }] }
})

这相当于替用户输入了一条继续的消息

### 3.4 openloop 的巧妙之处

386行代码实现完整自动循环，秘诀：
- 完全不碰 LLM 调用逻辑
- 只做两件事：监听事件 + 注入消息
- 依赖 OpenCode 已有的全部能力

## 4. V2 方案回顾与失败分析

### 4.1 V2 方案摘要

方案文件：speaker_labeling_plan_v2.md

核心设计：
- Python 总控所有流程
- Chunk切分（max_paragraphs_per_chunk: 12-20）
- 固定上下文窗口（lookback_paragraphs: 2-4, lookahead: 0-2）
- 6个Task流水线（初标注→短期摘要→长期摘要→角色库→实体合并→回填修正）
- 三层记忆（短期摘要、长期摘要、角色卡片）
- 实体分类（character / mystery / npc_group）

### 4.2 V2 失败原因

架构级硬伤：Python 决定 LLM 能看到什么

1. 固定窗口漏线索：说话人线索在第6段前，但只给前3段lookback
2. 摘要会漂移：错误层层传播（Task A→B→C）
3. LLM被动接受：无法主动要求更多上下文
4. 记忆不如原文：摘要会丢失信息
5. 系统复杂但效果受限于窗口瓶颈

## 5. 核心洞察

OpenCode 让 LLM 翻书，V2 让 LLM 看卡片。

V2（被动分类器）：
  Python切好小块上下文 → 塞给LLM → LLM在小块里找 → 不够→标unknown

OpenCode（主动代理）：
  LLM收到任务 → 自己决定读哪里 → 不够→再读 → 还不够→跳到前面章节 → 够了→输出

关键差异：
| 谁控制阅读 | V2: Python预切分 | OpenCode: LLM自主导航 |
| 上下文大小 | V2: 固定窗口 | OpenCode: 动态 |
| 跨章节关联 | V2: 仅靠摘要(会漂移) | OpenCode: 直接翻原文 |
| 不确定时 | V2: 标unknown放弃 | OpenCode: 继续读更多 |
| 错误传播 | V2: 摘要漂移 | OpenCode: 每次直接读原文 |
| 架构 | V2: 被动分类器 | OpenCode: 主动代理 |

## 6. 独立方案设计

### 6.1 设计原则

放弃预设上下文窗口，给 LLM 工具，让它自己翻书。

### 6.2 架构

label_novel.py (Python)
  工具层（给LLM用）：
    read_novel(start_line, end_line) → 读取小说指定行范围
    get_next_dialogue() → 获取下一批待标注对话
    submit_labels(speakers) → 写入labeled.txt
    search_novel(keyword) → 搜索关键词（可选）

  Agent 循环（核心）：
    while 还有未标注对话:
      1. get_next_dialogue() → 对话信息
      2. 构建 messages = [system, user]
      3. 内层 while:
         response = LLM.chat(messages, tools)
         if response 要调工具:
           执行工具 → 结果追加到 messages
           continue（让LLM看结果）
         else:
           解析标注结果
           break
      4. 写入 labeled.txt

### 6.3 核心依赖

pip install openai

兼容所有 OpenAI 兼容 API：
- OpenAI (gpt-4o)
- DeepSeek (deepseek-chat) ← 推荐
- 通义千问 (qwen-plus)
- Ollama 本地模型

### 6.4 工具定义（JSON Schema）

TOOLS = [
  {
    name: "read_novel",
    description: "读取小说文件的指定行范围，返回每行内容及行号",
    parameters: { start_line: int, end_line: int }
  },
  {
    name: "get_next_dialogue",
    description: "获取下一批待标注对话，返回对话内容、行号、进度",
    parameters: {}
  },
  {
    name: "submit_labels",
    description: "提交标注结果，按顺序提供说话人名字",
    parameters: { speakers: [string] }
  },
  {
    name: "search_novel",
    description: "在小说中搜索关键词，返回包含该词的行号列表",
    parameters: { keyword: string }
  }
]

### 6.5 系统提示词

你是一个小说对话标注助手。判断「」内的对话是谁说的。

工作流程：
1. 调用 get_next_dialogue 获取待标注对话
2. 调用 read_novel 阅读上下文
3. 如果不够，继续调用 read_novel 读更多
4. 需要时用 search_novel 搜索
5. 确信后调用 submit_labels 提交
6. 循环直到全部完成

判断规则：
- 根据某某说、某某道等叙述推断
- 一般对话轮换：A一句B一句
- 被称呼者通常不是说话者
- 无法确定标未知，不瞎猜
- 角色名保持统一

### 6.6 文件结构

novel-labeler/
  label_novel.py       - 主程序（约300行）
  novel.txt            - 用户放入的小说
  labeled.txt          - 自动生成：标注结果
  requirements.txt     - 内容: openai
  README.md            - 使用说明

### 6.7 CLI

python label_novel.py novel.txt                        # 基本用法
python label_novel.py novel.txt --model gpt-4o          # 指定模型
python label_novel.py novel.txt --model deepseek-chat   # DeepSeek(推荐)
    --base-url https://api.deepseek.com/v1
python label_novel.py novel.txt --model qwen2.5:32b     # 本地Ollama
    --base-url http://localhost:11434/v1
python label_novel.py novel.txt --batch-size 5          # 批量标注
python label_novel.py novel.txt --dry-run               # 干跑

### 6.8 成本估算（2000句对话）

DeepSeek V3: 约 10-15 元
GPT-4o-mini: 约 10-15 元
GPT-4o: 约 200-300 元

### 6.9 与原方案对比

| 维度 | OpenCode+openloop | 本方案 |
| 安装 | 安装OpenCode+插件 | pip install openai |
| 启动 | TUI输入命令 | python label_novel.py |
| 分发 | 需详细教程 | 一个README |
| 断点续传 | 依赖状态文件 | labeled.txt天然支持 |
| 准确率 | 高(Agent模式) | 同等高(同样Agent模式) |
| 适合人群 | 开发者 | 任何人 |

## 7. 从两个项目中可借鉴的技术

### 7.1 从 OpenCode 借鉴

read_file的offset/limit设计（tool/read.ts 第29-37行）→ read_novel的start_line/end_line
工具返回带行号格式（tool/read.txt）→ read_novel同样输出
Tool Registry合并工具模式（tool/registry.ts）→ 我们的TOOLS列表
Agent循环模式（session/prompt.ts 第522-700行）→ while+tool_calls+execute+continue

### 7.2 从 openloop 借鉴

状态文件管理（src/index.ts 第22行）→ labeled.txt天然记录
迭代上限保护（第23行）→ --max-iterations 参数

### 7.3 不需要的

OpenCode的TUI界面、会话管理、事件总线、插件系统
openloop的事件监听、promptAsync SDK调用

## 8. 实施计划

第一阶段（3-4h）：核心脚本
  1.1 创建项目结构
  1.2 对话提取逻辑（复用get_dialogue.py正则）
  1.3 工具函数（read_novel, get_next_dialogue, submit_labels, search_novel）
  1.4 Agent循环（OpenAI Function Calling + while循环）
  1.5 System Prompt设计

第二阶段（2h）：鲁棒性
  2.1 断点续传（labeled.txt行数=已标注数）
  2.2 错误重试（API失败自动重试3次）
  2.3 进度显示（[156/2340] 6.7%）
  2.4 Ctrl+C安全退出
  2.5 迭代上限保护

第三阶段（1h）：参数化
  3.1 argparse CLI
  3.2 批量标注支持
  3.3 干跑模式

第四阶段（2h）：测试和文档
  4.1 README.md
  4.2 用现有novel.txt测试
  4.3 边界测试

## 附录：代码参考索引

| 要写什么 | 参考哪里 | 参考什么 |
| 工具定义结构 | opencode-dev/packages/opencode/src/tool/tool.ts | Def接口和参数schema |
| read_file参数 | opencode-dev/packages/opencode/src/tool/read.ts | offset/limit设计 |
| read_file提示词 | opencode-dev/packages/opencode/src/tool/read.txt | 工具描述写法 |
| 工具注册执行 | opencode-dev/packages/opencode/src/session/prompt.ts | AI SDK格式转换 |
| openloop状态 | openloop-main/src/index.ts | 状态文件读写 |
| openloop循环 | openloop-main/src/index.ts | event→检查→注入消息 |
| get_dialogue | 当前目录 get_dialogue.py | 正则匹配和行号提取 |
| write_label | 当前目录 write_label.py | 追加写入 |
